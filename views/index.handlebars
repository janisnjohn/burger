<div class="container">
  <div class="row">
    <center><img id="ham" src="../assets/img/hamburger.png" width="200px">
    <h1 class="title">Eat-Da-Burger</h1></center>
  </div>

<div class="row">
  <div class="col-md-6">
    <h2 class="uneaten-title">Current Burgers</h2>
    <div class="uneaten">
      {{#each burgers}}
        {{#unless this.devoured}}
            <ol>
              {{this.id}}   {{this.burger_name}}
            </ol>
            <form class="form-inline" action="burgers/update/{{this.id}}?_method=PUT" method="POST">
              <input type="hidden" name="devoured" value="true">
              <button type="submit" class="btn btn-default" style="background-color:orange; color:yellow;">Devour It!</button>
            </form>
        {{/unless}}
      {{/each}}
    </div>
</div>

  <div class="col-md-6" id="eaten">
      <br><br><br>
      <h2 class="eaten-title">Eaten Burgers</h2>
      <div class="burgers-eaten">
        {{#each burgers}}
          {{#if this.devoured}}
            <ol>
               {{this.id}}   {{this.burger_name}}
            </ol>
            <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
             <input type="hidden" name="devoured" value="false">
            </form>
          {{/if}}
        {{/each}}
      </div>

    </div>
</div>

<div class="row">
  <div class="col-md-4"></div>
    <div class="col-md-4" id="update">
      <br><br><br>
        <form class="create-update-form" action="burgers/create" method="POST">
          <h2 class="add-title">Add a Burger</h2>
          <div class="form-group">
            <label for="newBurger">Burger Name:</label>
            <input type="hidden" name="devoured" value="0">
            <input type="text" id="newBurger" name="name">
          <button type="submit" class="btn btn-default" style="background-color:orange; color:yellow;">Add Burger</button>
        </form>
      </div>
</div>